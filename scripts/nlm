#!/usr/bin/env bash
# 始终调用系统 notebooklm（~/.local/bin），不受 venv 里旧版影响
set -euo pipefail
NLM="${NOTEBOOKLM_BIN:-$HOME/.local/bin/notebooklm}"
if [[ ! -x "$NLM" ]]; then
  echo "未找到 $NLM"
  echo "安装: uv tool install notebooklm-py"
  exit 1
fi
exec "$NLM" "$@"
